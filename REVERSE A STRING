REVERSE A STRING:

#include <stdio.h>  
#include <string.h>  
int main()  
{  
    char str[50];   
    scanf ("%s", str);  
    printf (" \n  The reverse of a string: %s ", strrev(str));  
    return 0;  
}  
